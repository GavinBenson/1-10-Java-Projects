
public class CreateMsg {
	
	// make a constructor public -- so other classes can access it
	public CreateMsg() {
		// placeholder
	}
	
	//Create a "method" (function)
	// scope   return type   name(lower case)   pass parameters
	public void msg(String message) {
		System.out.println(message);
	}
}
