Grade checkpoint #1: encoding

JOP
executable jar
JOP - ask do you want to encode, decode, or crack?
branch, rn only encode
for encoding - ask for plain text message
ask for the key
display the encoded message
loop - ask "Do you want to encode another message?"
either go again or exit




Grade checkpoint #2: decoding
1. All JOPs, executable jar
2. Select decode, enter the coded message
3. Enter key
4. Program decodes, JOP's the decoded msg
5. shift alpha to the left (subract key instead of add). z-wrap becomes the a-wrap.







Grade checkpoint #3: cracking
